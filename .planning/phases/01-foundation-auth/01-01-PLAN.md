# Plan 01-01: Project Setup & Infrastructure

---
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - jest.config.js
  - app.json
  - .env.example
  - src/types/auth.ts
  - src/types/user.ts
  - src/api/types.ts
  - tests/setup.ts
  - tests/__mocks__/expo-secure-store.ts
autonomous: yes
requirements:
  - US-AUTH-001
  - US-AUTH-002
  - US-AUTH-003
---

## Objective

Initialize the React Native/Expo project with TypeScript, configure testing framework, and establish core type definitions for authentication.

## Tasks

### Task 1: Initialize Expo Project with TypeScript

<read_first>
- .planning/PHASE01-RESEARCH.md (lines 52-67: installation commands and versions)
- .planning/REQUIREMENTS.md (lines 1-23: auth requirements)
- .planning/config.json (project configuration)
</read_first>

<action>
Initialize a new Expo project with TypeScript template and install core dependencies:

1. Create Expo app with TypeScript:
```bash
npx create-expo-app@latest . --template expo-template-blank-typescript
```

2. Install navigation dependencies:
```bash
npx expo install react-native-screens react-native-safe-area-context
npm install @react-navigation/native @react-navigation/native-stack
```

3. Install authentication dependencies:
```bash
npx expo install expo-auth-session expo-crypto expo-web-browser expo-secure-store
npm install axios jwt-decode react-hook-form zod @hookform/resolvers
npm install @react-native-async-storage/async-storage
```

4. Install dev dependencies:
```bash
npm install --save-dev jest @testing-library/react-native @testing-library/jest-native @types/jest jest-expo
```

5. Update app.json with scheme and bundle ID:
```json
{
  "expo": {
    "name": "Demus",
    "slug": "demus-mobile",
    "scheme": "demus",
    "version": "1.0.0",
    "ios": {
      "bundleIdentifier": "com.demus.music",
      "supportsTablet": true
    },
    "android": {
      "package": "com.demus.music",
      "adaptiveIcon": {
        "foregroundImage": "./assets/adaptive-icon.png",
        "backgroundColor": "#FFFFFF"
      }
    }
  }
}
```

6. Create .env.example with OAuth placeholders:
```
EXPO_PUBLIC_API_URL=https://api.demus.com
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your_google_client_id_here
EXPO_PUBLIC_APPLE_CLIENT_ID=your_apple_client_id_here
```
</action>

<verify>
Check project initialization:
```bash
test -f package.json && echo "✓ package.json exists"
test -f tsconfig.json && echo "✓ tsconfig.json exists"
test -f app.json && echo "✓ app.json exists"
test -f .env.example && echo "✓ .env.example exists"
npm list expo react-native typescript
```
</verify>

<acceptance_criteria>
- package.json contains "expo"
- package.json contains "typescript"
- package.json contains "@react-navigation/native"
- package.json contains "expo-secure-store" 
- package.json contains "expo-auth-session"
- package.json contains "axios"
- tsconfig.json exists
- app.json contains "scheme": "demus"
- app.json contains "bundleIdentifier": "com.demus.music"
- app.json contains "package": "com.demus.music"
- .env.example contains EXPO_PUBLIC_API_URL
- .env.example contains EXPO_PUBLIC_GOOGLE_CLIENT_ID
- .env.example contains EXPO_PUBLIC_APPLE_CLIENT_ID
</acceptance_criteria>

<done>false</done>

---

### Task 2: Configure Jest Testing Framework

<read_first>
- .planning/PHASE01-RESEARCH.md (lines 809-846: test framework configuration)
- package.json (verify jest dependencies installed)
</read_first>

<action>
Configure Jest for React Native testing:

1. Create jest.config.js:
```javascript
module.exports = {
  preset: 'jest-expo',
  setupFilesAfterEnv: ['<rootDir>/tests/setup.ts'],
  transformIgnorePatterns: [
    'node_modules/(?!((jest-)?react-native|@react-native(-community)?)|expo(nent)?|@expo(nent)?/.*|@expo-google-fonts/.*|react-navigation|@react-navigation/.*|@unimodules/.*|unimodules|sentry-expo|native-base|react-native-svg)'
  ],
  testMatch: ['**/__tests__/**/*.ts?(x)', '**/?(*.)+(spec|test).ts?(x)'],
  collectCoverageFrom: [
    'src/**/*.{ts,tsx}',
    '!src/**/*.d.ts',
    '!src/**/__tests__/**'
  ],
  coverageThreshold: {
    global: {
      branches: 80,
      functions: 80,
      lines: 80,
      statements: 80
    }
  },
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1'
  }
};
```

2. Create tests/setup.ts:
```typescript
import '@testing-library/jest-native/extend-expect';

// Mock expo-secure-store
jest.mock('expo-secure-store', () => require('./__mocks__/expo-secure-store'));

// Mock expo-auth-session
jest.mock('expo-auth-session');

// Mock expo-web-browser
jest.mock('expo-web-browser');

// Silence console errors in tests
global.console = {
  ...console,
  error: jest.fn(),
  warn: jest.fn(),
};
```

3. Create tests/__mocks__/expo-secure-store.ts:
```typescript
const store: Record<string, string> = {};

export const setItemAsync = jest.fn((key: string, value: string) => {
  store[key] = value;
  return Promise.resolve();
});

export const getItemAsync = jest.fn((key: string) => {
  return Promise.resolve(store[key] || null);
});

export const deleteItemAsync = jest.fn((key: string) => {
  delete store[key];
  return Promise.resolve();
});
```

4. Add test scripts to package.json (merge with existing scripts):
```json
"scripts": {
  "test": "jest",
  "test:watch": "jest --watch",
  "test:coverage": "jest --coverage",
  "test:auth": "jest --testPathPattern=auth"
}
```
</action>

<verify>
Check Jest configuration:
```bash
test -f jest.config.js && echo "✓ jest.config.js exists"
test -f tests/setup.ts && echo "✓ tests/setup.ts exists"
test -f tests/__mocks__/expo-secure-store.ts && echo "✓ mock file exists"
```
</verify>

<acceptance_criteria>
- jest.config.js exists with preset: 'jest-expo'
- jest.config.js contains setupFilesAfterEnv
- jest.config.js contains coverageThreshold.global.lines: 80
- tests/setup.ts exists and imports '@testing-library/jest-native/extend-expect'
- tests/__mocks__/expo-secure-store.ts exports setItemAsync, getItemAsync, deleteItemAsync
- package.json scripts section contains "test": "jest"
- package.json contains "jest-expo" in devDependencies
</acceptance_criteria>

<done>false</done>

---

### Task 3: Create Core Type Definitions

<read_first>
- .planning/PHASE01-RESEARCH.md (lines 543-563: auth service interface)
- .planning/REQUIREMENTS.md (lines 7-23: auth requirements)
</read_first>

<action>
Create TypeScript type definitions for authentication:

1. Create src/types/user.ts first (needed by auth.ts):
```typescript
export interface User {
  id: string;
  email: string;
  displayName: string;
  avatar?: string;
  createdAt: string;
  updatedAt: string;
}

export interface UserProfile extends User {
  bio?: string;
  favoriteGenres?: string[];
  followersCount: number;
  followingCount: number;
}
```

2. Create src/types/auth.ts:
```typescript
import type { User } from './user';

// Authentication request/response types
export interface LoginRequest {
  email: string;
  password: string;
}

export interface RegisterRequest {
  email: string;
  password: string;
  displayName: string;
}

export interface LoginResponse {
  accessToken: string;
  refreshToken: string;
  user: User;
}

export interface RefreshTokenRequest {
  refreshToken: string;
}

export interface RefreshTokenResponse {
  accessToken: string;
}

export interface OAuthRequest {
  code: string;
  idToken?: string;
}

export interface PasswordResetRequest {
  email: string;
}

export interface PasswordResetConfirmRequest {
  token: string;
  password: string;
}

export interface ChangePasswordRequest {
  currentPassword: string;
  newPassword: string;
}

export interface JWTPayload {
  exp: number;
  iat: number;
  userId: string;
  email: string;
  tokenVersion?: number;
}

export interface AuthState {
  user: User | null;
  loading: boolean;
  error: string | null;
}
```

3. Create src/api/types.ts:
```typescript
export interface ApiResponse<T> {
  success: boolean;
  data: T;
  message?: string;
}

export interface ApiError {
  success: false;
  error: {
    code: string;
    message: string;
    details?: Record<string, any>;
  };
}

export const API_ENDPOINTS = {
  AUTH: {
    LOGIN: '/auth/login',
    REGISTER: '/auth/register',
    LOGOUT: '/auth/logout',
    REFRESH: '/auth/refresh',
    FORGOT_PASSWORD: '/auth/forgot-password',
    RESET_PASSWORD: '/auth/reset-password',
    CHANGE_PASSWORD: '/auth/change-password',
    GOOGLE: '/auth/google',
    APPLE: '/auth/apple',
  },
  USER: {
    PROFILE: '/user/profile',
    UPDATE_PROFILE: '/user/profile',
  },
} as const;

export type HttpMethod = 'GET' | 'POST' | 'PUT' | 'PATCH' | 'DELETE';

export interface RequestConfig {
  method: HttpMethod;
  headers?: Record<string, string>;
  params?: Record<string, any>;
  timeout?: number;
}
```

4. Create src/types/index.ts:
```typescript
export * from './auth';
export * from './user';
```
</action>

<verify>
Check type definition files:
```bash
test -f src/types/auth.ts && echo "✓ auth types exist"
test -f src/types/user.ts && echo "✓ user types exist"
test -f src/api/types.ts && echo "✓ api types exist"
test -f src/types/index.ts && echo "✓ barrel export exists"
npx tsc --noEmit
```
</verify>

<acceptance_criteria>
- src/types/auth.ts exists and exports LoginRequest, RegisterRequest, LoginResponse, JWTPayload, AuthState
- src/types/user.ts exists and exports User, UserProfile
- src/api/types.ts exists and exports ApiResponse, ApiError, API_ENDPOINTS, HttpMethod
- src/types/index.ts exists and re-exports from auth and user
- API_ENDPOINTS.AUTH.LOGIN equals '/auth/login'
- API_ENDPOINTS.AUTH.GOOGLE equals '/auth/google'
- API_ENDPOINTS.AUTH.APPLE equals '/auth/apple'
- Command `npx tsc --noEmit` exits 0 (no TypeScript errors)
</acceptance_criteria>

<done>false</done>

---

## Verification

### Must-Haves
- [ ] Expo project initialized with TypeScript
- [ ] All core dependencies installed
- [ ] Jest configured with React Native preset
- [ ] Test mocks created for Expo modules
- [ ] Type definitions created for auth, user, API
- [ ] app.json configured with scheme and bundle IDs
- [ ] Environment variable template created

### Success Criteria
- `npx tsc --noEmit` exits 0
- All 3 type definition files exist in src/types/
- jest.config.js has 80% coverage threshold
- .env.example has OAuth client ID placeholders

### Integration Points
- Types imported by authService (Plan 01-02)
- Jest config used by test files in all plans
- app.json scheme enables OAuth redirect URIs (Plan 01-04)

---

*Phase: 01-foundation-auth | Plan: 01-01 | Wave: 1*
